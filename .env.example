# Copy to .env for local development: cp .env.example .env
# The server loads .env on startup (see cmd/server/config.go).

DATABASE_URL=postgres://test:test@localhost:5432/transactions?sslmode=disable
LISTEN_ADDR=:8080